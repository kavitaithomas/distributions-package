# setup file is necessary for pip installing
# includes descriptive metadata

from setuptools import setup

setup(name='b-g-dist',
      version='0.1',
      description='Gaussian and Binomial Distributions',
      packages=['b-g-dist'],
      author='Kavita Thomas',
      zip_safe=False)



